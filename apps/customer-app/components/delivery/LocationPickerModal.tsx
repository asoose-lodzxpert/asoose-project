// Modal-based picker removed — route-based `location-picker` screen is used instead.
// Keep a tiny stub export to avoid breaking any stray imports while you remove them.
export function LocationPickerModal() {
  return null;
}
